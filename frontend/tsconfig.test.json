{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",

    /* Some unit tests read map fixtures off disk (fs/path/url), so Node's
       ambient types are needed on top of the app project's vite/client. */
    "types": ["vite/client", "node"]
  },
  "include": ["src"],
  "exclude": []
}
