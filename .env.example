# Copy to .env.local for local dev.
# Mock mode (default) runs the whole frontend standalone with no backend.
VITE_USE_MOCK=1
# Set to 0 and point at the backend to use the real Socket.IO + LiveKit server:
# VITE_USE_MOCK=0
# VITE_SERVER_URL=http://localhost:3001
