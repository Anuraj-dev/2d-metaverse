# 2026-07-05

## PRD-sweep close-out + user bug/feature batch (5 PRs shipped & merged)
- did: ran 5 parallel coder agents (Opus xhigh — the last spawns before the effort-policy change below) + Codex review loops; all five PRs merged to main, zero open PRs left.
  - **#58 (PRD 17 stage broadcast) — the STATE.md "last bug" closed.** The room-access E2E failure was NOT the hypothesized media-routing hang: the Playwright trace proved E2E-harness fragility — the `walkTo` stuck-detector misread CI render freezes (>4s positions-tick gaps) as navigation stalls, plus room-access.spec was the only two-context spec still on a 60s timeout. Fixed in the harness (gap-discontinuity reset + 120s budget); product code untouched. Two review rounds also purged stale `STAGE_KEY` config/docs.
  - **#59 (PRD 16 arcade zone)**: fixed stale fullscreen assertion + raised door-path corridor waypoint tolerance to 12 (walkTo damping-band flake; map geometry was NOT the cause).
  - **#61 room audio bug** ("teammate inaudible in some rooms"): root cause = 200px `AUDIO_CUTOFF` < every room interior (hostel 3 diagonal 280px) — same-room players past 200px went silent. Fix: `ROOM_AUDIO_FLOOR = 0.35` within a shared room zone; outdoor falloff + no-voice-through-walls unchanged. roomBounds rects were verified correct (flood-fill test added to maps.test.ts).
  - **#60 board-seat double-sit**: server FSM was already correct; client sat optimistically and never rolled back on `seat-taken`. Added pure `boardTable.ts` occupancy helpers, authoritative-snapshot reconciliation, "Seat taken" hint; manager-tier same-seat race regression test.
  - **#62 in-meeting text chat** (new feature): participant-scoped chat in the meeting overlay; payload carries only `text` (meeting derived server-side), per-participant fan-out (room occupants can't eavesdrop), ephemeral, entry-bundle-neutral (chat state lives in the lazy MeetingOverlay). Codex round 1 caught a real race — `chat()` read meeting state outside the per-room serialized queue; fix serializes the gate+fan-out through the same queue, with a dispatched-but-unapplied-window regression test.
- changed (process): coder effort policy — orchestrator now picks effort per task via agent variants `opus-coder` (medium, default) / `opus-coder-high` / `opus-coder-xhigh`, compensating with detailed `/prompt-craft` specs. Review-fix rounds go to fresh Fable agents (standing consent).
- gotcha: home-router DNS dropped `api.github.com` for ~20 min mid-session (other hosts fine); background `codex exec` runs also died silently around then. Foreground codex + a DNS-recovery monitor worked around it.
- next: nothing queued. PRDs 13–17 all shipped. Prod backend may still be stale (~June 20) — verify before relying; FE auto-deployed from today's merges.
