# Sessions — 2026-07-09

## 02:20 — Meeting bug fix + PRD 23 shipped, PRD 21 in review, PC-crash rule
- did: Diagnosed + fixed the meeting-countdown race (PR #74, merged, backend deployed to prod). Shipped PRD 23 + Raja's fixes (PR #75, merged, Vercel prod): screen-share publish (meeting-gated) w/ focus-tile grid, meeting-chat toggle + unread, clean top bar, removed BubbleLayer video-balls, knock/approval prompts visible in meetings, HelpOverlay overlap fix. PRD 21 implemented by a Sonnet-5-high coder → PR #76 OPEN, Codex review pending (first attempt died in background; rerun was aborted when Raja's PC crashed). Streak-saver commit 84223eb. Started Sonnet-vs-Opus experiment (agent defs + memory). Broadened merge authorization recorded.
- changed: backend/src/meeting.ts (+tests); ~29 frontend files for PRD 23; frontend media/mediaLogic.ts, soundMixer.ts, sfx.ts, settings.ts, curate_audio.py (+tests) on branch prd-21/audio-feel; .claude/agents/sonnet-*.md; memories.
- decided: HARD RULE — no full local build/test runs by anyone (crashed the PC); CI is the only gate; codex reviews from diff + `gh pr checks`. → decisions.md.
- next: Codex review of PR #76 (diff + CI checks only, foreground codex), merge on ✅ + green CI, then PRD 22 (#67) via sonnet-coder-high, then browser E2E pass of the meeting features.

## 04:30 — Overnight autonomous run: PRDs 21+22 shipped, full prod QA pass
- did: Codex-reviewed PR #76 (0 findings) → merged. Committed the docs checkpoint. Dispatched PRD 22 to an Opus-4.8-medium coder (Sonnet experiment paused by Raja) → PR #77; codex found 1 blocking (seed hardcoded room names) → Fable fix agent derived them from `roomDisplayName` (commit 01eaff2) → codex re-approved → merged. FE+BE auto-deployed (prod = 4acf21f). Ran the full prod browser E2E pass via a Sonnet scout: ALL checklist items PASS; 4 findings recorded in STATE (LiveKit /rtc/v1 404 retry loop [medium], settings/map overlay stacking, control-bar/filmstrip overlap, prod URL undocumented). Scout first landed on the WRONG app (`2d-metaverse.vercel.app` is third-party) — real prod is https://space.raja-dev.me only (CORS).
- changed: PRs #76/#77 merged to main; docs/ committed (98b2de6); memory `sonnet-experiment` marked PAUSED.
- decided: Browser QA of the world must use headless Playwright pages (Claude-in-Chrome tabs are hidden → Phaser pauses). → decisions.md
- next: Raja triages QA findings (LiveKit server upgrade first), closes/files issues #65–#68 + QA bugs (classifier blocks the agent), picks the next PRD.
